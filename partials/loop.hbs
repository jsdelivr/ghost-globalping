{{#get "posts" filter="tag:-content" include="tags" limit=@config.posts_per_page page=pagination.page }}
	<section class="post-feed" aria-label="Articles">
		{{#foreach posts}}
			{{> "post-card" priority=@first}}
		{{/foreach}}
	</section>
	{{pagination}}
{{/get}}
