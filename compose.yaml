services:
  ghost:
    image: ghost:5.94-alpine
    ports:
      - "2368:2368"
    environment:
      NODE_ENV: development
      url: http://localhost:2368
    volumes:
      - ghost-content:/var/lib/ghost/content
      - .:/var/lib/ghost/content/themes/globalping

volumes:
  ghost-content:
